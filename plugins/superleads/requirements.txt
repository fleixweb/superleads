# Runtime and validator dependencies for development and deterministic exports.
# Install only in an isolated project or maintenance environment, never globally or in a managed plugin cache.
jsonschema==4.25.1
referencing==0.37.0
openpyxl==3.1.5
